(ns cdlib.users.collection.create-mass
  (:require [cdlib.artists.discs.model :as db]
            [formant.core :as formant]
            [formant.validators :as validators]))

(defn perform [params user-id])
